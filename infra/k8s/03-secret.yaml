apiVersion: v1
kind: Secret
metadata:
  name: zmnnoory-secret
  namespace: zmnnoory
  labels:
    app: zmnnoory-backend
type: Opaque
data:
  # 주의: 아래 값들은 base64로 인코딩된 값입니다
  # 실제 배포 시에는 GitLab Variables에서 가져온 값으로 교체해야 합니다
  
  # JWT 시크릿 키 (base64 인코딩 필요)
  # echo -n "your-actual-jwt-secret-key" | base64
  JWT_SECRET_KEY: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24tdGhpcy1pcy1ub3Qtc2VjdXJlLWVub3VnaC1mb3ItcHJvZHVjdGlvbi11c2U=
  
  # 데이터베이스 비밀번호 (base64 인코딩 필요)  
  # echo -n "q1w2e3r4t5" | base64
  DB_PASSWORD: cTF3MmUzcjR0NQ==
  
  # Redis 비밀번호 (필요한 경우)
  # echo -n "redis-password" | base64
  REDIS_PASSWORD: "" 